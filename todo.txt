Key:
    +: file / module
    -: an item to todo
    ?: maybe do
    X: to do if possible (which it may not be)
    ~: deferred for far later


To Do's:
- document each file
- clean up documentation
- run rustfmt
+ lib.rs:
    ? change gen functions to assoc fns of the wrapper they make
    - check inlining
    - check get's process updated index parameters
    - clear up entry v. element v. item v. etc.
    - clear up buffer v. slot in documentation
    - fold & fold_ref clarifications
    - rework vectorizing_matrix_structs.rs
    ~ figure out vectored matrices (seems like a really niche optimization which brings tons of complexity)
    ~ figure out hinting / encouraging SIMD 
    + test:
        - Update mat_vec_mul to use matrix types
    + matrix.rs:
        - add MatrixOps extension(s) for evaluation / consumption
        - Add Matrix and Vector multiplication
        - remove into_2d_array (not Into<[[T; D1]; D2]>)
        - replace into_2d_heap_array w/ `Box<MatrixExpr<_>>: Into<Box<[[T; D1]; D2]>>`
        - add assign ops to MathMatrix
        ? rename ArrayMatrixOps
        - add heap_make_repeatable
        - add runtime sized wrappers
        - add wrap shortcut macro to MatrixOps
        - generalize rref outside of f64 
            - adapt more complex num bounds to use premade ones from a crate
        - check pivot management of rref & det
        ~ check numerical stability of det && rref

        + mat_util_traits.rs:
            ? remove transposition information
        + matrix_builders.rs:
        + matrix_structs.rs:
    + vector.rs:
        - add VectorOps extension(s) for evaluation / consumption
        - Add Vector and Matrix multiplication
        X Add addassign and subassign for generic V2: VectorOps
        ? Add heap_make_repeatable
        - add wrap shortcut macro to VectorOps 
        X move make_dynamic to VectorOps
        X merge ArrayVectorOps & SliceVectorOps into VectorOps using Builder bounds
        - figure out implementing Clone for RSMathVector

        + vec_util_traits.rs:
            ? VectorBuilder: Clone --> VectorBuilder: Copy
        + vector_builders.rs:
        + vector_structs.rs:
            - check VecAttachUsedVec dropping
            - make the assign structs maintain the &'a mut T item
            - add an OwnedSlice struct

Fix Me's:
+ lib.rs:
    + test:
    + matrix.rs:
        + mat_util_traits.rs:
        + matrix_builders.rs:
        + matrix_structs.rs:
            - check vectorizing matrix leaks
    + vector.rs:
        - check attach_buf note
        + vec_util_traits.rs:
        + vector_builders.rs:
        + vector_structs.rs:
            - require HalfBind to have an owned buffer