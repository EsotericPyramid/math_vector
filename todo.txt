Key:
    +: file / module
    -: an item to todo
    ?: maybe do
    X: to do if possible (which it may not be)
    ~X: to do *when* possible (ie. definitely not possible now)
    ~: deferred for far later


To Do's:
- establish if vectors / matrices need to have at least 1 dimension
- document each file
- clean up documentation
- define when to use operation traits vs. broader num/field traits
~ add some sort of system for subspaces
~ add vector/matrix exprs which mirror files
~ add safety explanations (and check safety along the way)
~ clean up types to make them more readable
? rename things to make more sense
+ lib.rs:
    X get heaped things to work outside of `--release`
    ? rename Fst & Snd buffer to Active and Reserve buffer (or similar)
    X change gen functions to assoc fns of the wrapper they make
    X deal with transmute_copy when copies aren't actually needed (used in that case to dodge compiler errors)
    - adapt more complex num bounds to use premade ones from a crate
    - check inlining
    - check get's process updated index parameters
    - clear up entry v. element v. item v. etc. in documentation / names
    - clear up buffer v. slot in documentation
    - fold & fold_ref clarifications
    - rework vectorizing_matrix_structs.rs
    ~ figure out vectored matrices (seems like a really niche optimization which brings tons of complexity)
    ~ figure out hinting / encouraging SIMD 
    + test:
    + matrix.rs:
        ~X finish implementing VectorizedMatrix (reqs more advanced wrap sizing)
        - unchecked RSMathMatrix conversions
        X option to use either kind of matrix slice for the generic RSMathExpr impl
        - generalized AttachBuf struct
        X generalize RSMathVector indexing (Dope Matrix col indexing)
        ? rename ArrayMatrixOps
        ? add heap_make_repeatable
        ? add wrap shortcut macro to MatrixOps
        - convert assign impls to use the builder system instead of conditionals
        - check pivot management of rref & det
        ~ check numerical stability of det && rref

        + mat_util_traits.rs:
            ? remove transposition information
        + matrix_builders.rs:
        + matrix_structs.rs:
    + vector.rs:
        ? remove RefRSMathVector *type alias* (just the alias, nothing else)
        - document repeated 
        - splitup and reorganize vector.rs
        X fix RS Vec's trait bound infinite recursion when evaluating sometimes (ex: &RSMathVector * 3 with unspecified num type)
        X convert assign impls to use the builder system instead of conditionals (non-trivial because of HRTBs I think)
        X add a VectorBuffer trait: declares if a buffer can be created on a vector expr
            - could allow more general eval implementation
            - currently runs into collision between FstHandleBool = N and FstHandleBool = Y
                - MaybeCreates could be used but muck up the buffer type
        X remove &Box and &mut Box for impls VectorExpr (should be redundant because of Borrow but isn't fully)
        ? generic MaybeCreate wrapper
        X improve VectorEvalOps to allow differentiation of attempted buffer types
        X Add addassign and subassign for generic V2: VectorOps
        ? Add heap_make_repeatable
        ? add wrap shortcut macro to VectorOps 
        ? merge ArrayVectorOps into VectorOps using Builder bounds

        + vec_util_traits.rs:
            X remove where bounds from `ConcreteVectorExpr`'s `borrow` and `borrow_mut` methods
        + vector_builders.rs:
            ? impl remaining VectorBuilderUnion's for VectorInnerProdExprBuilder
        + vector_structs.rs:
        + vector_math.rs:
            - impl VectorInPlaceEval for VectorInnerProdExpr
            X move the InnerProduct's vector generics out of the trait def and into the fn def
            ? move ConcreteVectorExpr into vector.rs
            - add Inner-product-space-relevant operations (so proj (onto_vec), orthogonal_part, into_orthonormal_basis, proj_onto_subspace, adjoint)
            - add Inner product defined on an orthonomal basis
            - add change-of-basis operation
            - add eigenvalue / eigenvector algorithms (+ diagonalizing)
    + tests.rs:
        - improve test suite, needs more *unit* tests, most currently just test that everything works on compile or benchmark performance

Fix Me's:
+ lib.rs:
    + test:
    + matrix.rs:
        + mat_util_traits.rs:
        + matrix_builders.rs:
        + matrix_structs.rs:
            - check vectorizing matrix leaks
    + vector.rs:
        - check attach_buf note
        + vec_util_traits.rs:
        + vector_builders.rs:
        + vector_structs.rs:
            - require HalfBind to have an owned buffer