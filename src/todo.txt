Key:
    +: file / module
    -: an item to todo
    ?: maybe do / may not be possible to do


To Do's:
- document each file
- clean up documentation
+ lib.rs:
    - check inlining
    - check get's process updated index parameters
    - clear up entry v. element v. item v. etc.
    - clear up buffer v. slot in documentation
    - fold & fold_ref clarifications
    - figure out vectored matrices
    + test:
        - Update mat_vec_mul to use matrix types
    + matrix.rs:
        - add MatrixOps extension(s) for evaluation / consumption
        - Add Matrix and Vector multiplication
        ? remove binding_get
        ? remove raw_next
        - remove into_2d_array (not Into<[[T; D1]; D2]>)
        - replace into_2d_heap_array w/ `Box<MatrixExpr<_>>: Into<Box<[[T; D1]; D2]>>`
        - add assign ops to MathMatrix
        - add map_bind
        - reimplement mat_mul method to use new builder systems
        - add specific fold methods
        ? rename ArrayMatrixOps
        - add heap_make_repeatable
        - add runtime sized wrappers
        - add index generator
        - add identity mat generator

        + mat_util_traits.rs:
            ? remove transposition information
        + matrix_builders.rs:
        + matrix_structs.rs:
    + vector.rs:
        - add VectorOps extension(s) for evaluation / consumption
        - Add Vector and Matrix multiplication
        - change T's refering to VectorLikes to V's
        ? remove binding_get?
        - Add/convert a runtime sized VectorIter
        - remove into_array (not Into<[T; D]>)
        - replace into_heap_array w/ `Box<VectorExpr<_>>: Into<Box<[T; D]>>`
        ? Add addassign and subassign for generic V2: VectorOps
        - Finish implementing RSVectorExpr
        ? remove initialized_sqr_mag (& copied_initialized_sqr_mag)
        ? remove initialized_dot
        - Add heap_make_repeatable
        - add copied fold methods
        - add copied dot

        + vec_util_traits.rs:
            ? VectorBuilder: Clone --> VectorBuilder: Copy
        + vector_builders.rs:
        + vector_structs.rs:
            - make the assign structs maintain the &'a mut T item
            - add an OwnedSlice struct

Fix Me's:
+ lib.rs:
    + test:
    + matrix.rs:
        + mat_util_traits.rs:
        + matrix_builders.rs:
        + matrix_structs.rs:
            - check vectorizing matrix leaks
    + vector.rs:
        - check attach_buf note
        + vec_util_traits.rs:
        + vector_builders.rs:
        + vector_structs.rs:
            - require HalfBind to have an owned buffer